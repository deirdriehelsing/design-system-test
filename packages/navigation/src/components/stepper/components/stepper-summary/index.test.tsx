import React from 'react';
import StepperSummary from '.';
import { render } from '@testing-library/react';

describe('<StepperSummary />', () => {
  it('renders', () => {
    render(<StepperSummary />);
  });
});
