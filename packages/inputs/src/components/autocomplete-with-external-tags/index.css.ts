import { style } from '@vanilla-extract/css';

const deleteIcon = style({
  color: 'var(--ref-palette-main-shade0)',
});

const chip = style({
  margin: '0 !important',
});

export { deleteIcon, chip };
