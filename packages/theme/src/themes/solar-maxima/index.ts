import createTheme from '../../helpers/create-theme';
import { solarMaximaTokens as tokens } from '@blueshift-ui/tokens';

const solarMaximaThemeConfig = createTheme({ tokens });

export default solarMaximaThemeConfig;
