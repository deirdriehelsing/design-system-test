import { NspRichContent, useNspContent } from '.';

describe('helpers index', () => {
  describe('NspRichContent', () => {
    it('is defined', () => {
      expect(NspRichContent).toBeDefined();
    });
  });

  describe('useNspContent', () => {
    it('is defined', () => {
      expect(useNspContent).toBeDefined();
    });
  });
});
