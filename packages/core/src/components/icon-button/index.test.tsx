import IconButton from '.';
import React from 'react';
import { render } from '@testing-library/react';

describe('<IconButton />', () => {
  it('renders', () => {
    render(<IconButton />);
  });
});
