import { Accordion, AppBar, MediaCard } from '.';

describe('package index', () => {
  describe('<Accordion />', () => {
    it('is defined', () => {
      expect(Accordion).toBeDefined();
    });
  });

  describe('<AppBar />', () => {
    it('is defined', () => {
      expect(AppBar).toBeDefined();
    });
  });

  describe('<MediaCard />', () => {
    it('is defined', () => {
      expect(MediaCard).toBeDefined();
    });
  });
});
