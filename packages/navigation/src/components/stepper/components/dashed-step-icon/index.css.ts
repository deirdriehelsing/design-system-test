import { style } from '@vanilla-extract/css';

const dash = style({
  background: 'currentcolor',
  borderRadius: 'var(--comp-stepLabel-children-dashIcon-borderRadius-default)',
  height: 'var(--comp-stepLabel-children-dashIcon-height-default)',
  width: '100%',
});

export { dash };
