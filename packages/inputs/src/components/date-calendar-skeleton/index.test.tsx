import DateCalendarSkeleton from '.';
import React from 'react';
import { render } from '@testing-library/react';

describe('<DateCalendarSkeleton />', () => {
  it('renders', () => {
    render(<DateCalendarSkeleton />);
  });
});
