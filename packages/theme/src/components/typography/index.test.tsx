import React from 'react';
import Typography from '.';
import { render } from '@testing-library/react';

describe('<Typography />', () => {
  it('renders', () => {
    render(<Typography />);
  });
});
