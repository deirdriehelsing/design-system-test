{
  "name": "@blueshift-ui/nsp",
  "version": "4.61.0",
  "description": "Hooks and components for interacting with NSP",
  "homepage": "https://github.com/varsitytutors/blueshift-ui/tree/master/packages/nsp#readme",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "sideEffects": [
    "**/*.css.ts",
    "**/*.css.js"
  ],
  "types": "dist/types/index.d.ts",
  "dependencies": {
    "@blueshift-ui/fetch": "4.61.0",
    "react-datocms": "7.2.2"
  },
  "peerDependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "transpile": "yarn clean && tsc",
    "transpile-changes": "tsc -b",
    "yalc-push": "yarn transpile && yalc push"
  }
}
